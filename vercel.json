{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/wallpaper/:slug", "destination": "/api/seo/wallpaper/:slug" },
    { "source": "/(.*)", "destination": "/" }
  ]
}
