{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/sitemap.xml", "destination": "/api/sitemap" },
    { "source": "/api/seo/wallpaper/:slug", "destination": "/api/seo/wallpaper/:slug" },
    { "source": "/(.*)", "destination": "/" }
  ]
}
